class ChatModel {

  String? name;
  String? icon;
  bool isGroup;
  String? time;
  String? currentMessage;

  ChatModel({this.name, this.icon,required this.isGroup, this.time, this.currentMessage});

}